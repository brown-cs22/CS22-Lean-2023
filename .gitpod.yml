image:
  file: .docker/gitpod/Dockerfile

vscode:
  extensions:
    - leanprover.lean4

tasks:
  - init: |
      lake exe cache get
      wget https://cs.brown.edu/~rlewis13/save-to-firestore-0.13.1.vsix -P /extensions/
    command: |
      PATH="$PATH:/workspace/leanclass/scripts"

  
